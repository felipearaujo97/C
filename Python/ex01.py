#Entrar via teclado com a base e a altura de um retângulo, calcular e exibir sua área.

b = float(input("Digite a Base: "))
al = float(input("Digite a Altura: "))
 
a = (b * al) 

print(f"A area do retângulo é: {a:.2f}")

